/** Kept beside package.json by test/version.test.ts, because a bundle
 *  cannot read package.json and an operator has to be able to ask a
 *  running deployment which build it is. */
export const VERSION = "3.10.0"
